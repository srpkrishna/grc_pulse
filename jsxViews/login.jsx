define(function (require) {
    var input = require("./common/input");
    var header = React.createClass({
        displayName: 'Login',
        render: function () {
            return (
                <div>
                    <div>
                        {getString("MetricStream")}
                    </div>
                    <div>

                    </div>
                </div>
            );
        }
    });
});